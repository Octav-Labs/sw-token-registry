// src/index.test.ts

import { foo } from '../src/index';

describe('Math functions', () => {
  test('add should return the sum of two numbers', () => {
    expect(foo).toBe('bar'); // Expected output: 5
  });
});
